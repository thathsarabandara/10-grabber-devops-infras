apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: robot-platform
  labels:
    app: mysql
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: grabber-platform
spec:
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    app: mysql
  type: ClusterIP
