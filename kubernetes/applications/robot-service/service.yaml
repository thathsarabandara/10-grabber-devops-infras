apiVersion: v1
kind: Service
metadata:
  name: robot-service
  namespace: robot-platform
  labels:
    app: robot-service
    app.kubernetes.io/name: robot-service
    app.kubernetes.io/part-of: grabber-platform
spec:
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  selector:
    app: robot-service
  type: ClusterIP
