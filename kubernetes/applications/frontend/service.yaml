apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: robot-platform
  labels:
    app: frontend
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: grabber-platform
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: frontend
  type: ClusterIP
