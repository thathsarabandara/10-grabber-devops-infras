apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: robot-service-monitor
  namespace: robot-platform
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: robot-service
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
